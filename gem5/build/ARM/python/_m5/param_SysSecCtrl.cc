/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/SysSecCtrl.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/arm/ssc.hh"

#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_SysSecCtrl");
    py::class_<SysSecCtrlParams, BasicPioDeviceParams, std::unique_ptr<SysSecCtrlParams, py::nodelete>>(m, "SysSecCtrlParams")
        .def(py::init<>())
        .def("create", &SysSecCtrlParams::create)
        .def_readwrite("compid0", &SysSecCtrlParams::compid0)
        .def_readwrite("compid1", &SysSecCtrlParams::compid1)
        .def_readwrite("compid2", &SysSecCtrlParams::compid2)
        .def_readwrite("compid3", &SysSecCtrlParams::compid3)
        .def_readwrite("ssc_dbgcfg_stat", &SysSecCtrlParams::ssc_dbgcfg_stat)
        .def_readwrite("ssc_pid0", &SysSecCtrlParams::ssc_pid0)
        .def_readwrite("ssc_pid1", &SysSecCtrlParams::ssc_pid1)
        .def_readwrite("ssc_pid2", &SysSecCtrlParams::ssc_pid2)
        .def_readwrite("ssc_pid4", &SysSecCtrlParams::ssc_pid4)
        .def_readwrite("ssc_version", &SysSecCtrlParams::ssc_version)
        ;

    py::class_<gem5::SysSecCtrl, gem5::BasicPioDevice, std::unique_ptr<gem5::SysSecCtrl, py::nodelete>>(m, "gem5_COLONS_SysSecCtrl")
        ;

}

static EmbeddedPyBind embed_obj("SysSecCtrl", module_init, "BasicPioDevice");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummySysSecCtrlParamsClass
{
  public:
    gem5::SysSecCtrl *create() const;
};

template <class CxxClass, class Enable=void>
class DummySysSecCtrlShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummySysSecCtrlShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const SysSecCtrlParams &>>>
{
  public:
    using Params = SysSecCtrlParams;
    static gem5::SysSecCtrl *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummySysSecCtrlShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const SysSecCtrlParams &>>>
{
  public:
    using Params = DummySysSecCtrlParamsClass;
    static gem5::SysSecCtrl *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::SysSecCtrl *
DummySysSecCtrlShunt<gem5::SysSecCtrl>::Params::create() const
{
    return DummySysSecCtrlShunt<gem5::SysSecCtrl>::create(*this);
}

} // namespace gem5
