/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ArmEmuFreebsd.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/arm/freebsd/se_workload.hh"

namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ArmEmuFreebsd");
    py::class_<ArmEmuFreebsdParams, ArmSEWorkloadParams, std::unique_ptr<ArmEmuFreebsdParams, py::nodelete>>(m, "ArmEmuFreebsdParams")
        .def(py::init<>())
        .def("create", &ArmEmuFreebsdParams::create)
        ;

    py::class_<gem5::ArmISA::EmuFreebsd, gem5::ArmISA::SEWorkload, std::unique_ptr<gem5::ArmISA::EmuFreebsd, py::nodelete>>(m, "gem5_COLONS_ArmISA_COLONS_EmuFreebsd")
        ;

}

static EmbeddedPyBind embed_obj("ArmEmuFreebsd", module_init, "ArmSEWorkload");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyArmEmuFreebsdParamsClass
{
  public:
    gem5::ArmISA::EmuFreebsd *create() const;
};

template <class CxxClass, class Enable=void>
class DummyArmEmuFreebsdShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyArmEmuFreebsdShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ArmEmuFreebsdParams &>>>
{
  public:
    using Params = ArmEmuFreebsdParams;
    static gem5::ArmISA::EmuFreebsd *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyArmEmuFreebsdShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ArmEmuFreebsdParams &>>>
{
  public:
    using Params = DummyArmEmuFreebsdParamsClass;
    static gem5::ArmISA::EmuFreebsd *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ArmISA::EmuFreebsd *
DummyArmEmuFreebsdShunt<gem5::ArmISA::EmuFreebsd>::Params::create() const
{
    return DummyArmEmuFreebsdShunt<gem5::ArmISA::EmuFreebsd>::create(*this);
}

} // namespace gem5
