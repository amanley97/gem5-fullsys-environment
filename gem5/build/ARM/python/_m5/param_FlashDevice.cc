/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/FlashDevice.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/arm/flash_device.hh"

#include "base/types.hh"
#include "base/types.hh"
#include "enums/DataDistribution.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_FlashDevice");
    py::class_<FlashDeviceParams, AbstractNVMParams, std::unique_ptr<FlashDeviceParams, py::nodelete>>(m, "FlashDeviceParams")
        .def(py::init<>())
        .def("create", &FlashDeviceParams::create)
        .def_readwrite("GC_active", &FlashDeviceParams::GC_active)
        .def_readwrite("blk_size", &FlashDeviceParams::blk_size)
        .def_readwrite("data_distribution", &FlashDeviceParams::data_distribution)
        .def_readwrite("erase_lat", &FlashDeviceParams::erase_lat)
        .def_readwrite("num_planes", &FlashDeviceParams::num_planes)
        .def_readwrite("page_size", &FlashDeviceParams::page_size)
        .def_readwrite("read_lat", &FlashDeviceParams::read_lat)
        .def_readwrite("write_lat", &FlashDeviceParams::write_lat)
        ;

    py::class_<gem5::FlashDevice, gem5::AbstractNVM, std::unique_ptr<gem5::FlashDevice, py::nodelete>>(m, "gem5_COLONS_FlashDevice")
        ;

}

static EmbeddedPyBind embed_obj("FlashDevice", module_init, "AbstractNVM");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyFlashDeviceParamsClass
{
  public:
    gem5::FlashDevice *create() const;
};

template <class CxxClass, class Enable=void>
class DummyFlashDeviceShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyFlashDeviceShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const FlashDeviceParams &>>>
{
  public:
    using Params = FlashDeviceParams;
    static gem5::FlashDevice *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyFlashDeviceShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const FlashDeviceParams &>>>
{
  public:
    using Params = DummyFlashDeviceParamsClass;
    static gem5::FlashDevice *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::FlashDevice *
DummyFlashDeviceShunt<gem5::FlashDevice>::Params::create() const
{
    return DummyFlashDeviceShunt<gem5::FlashDevice>::create(*this);
}

} // namespace gem5
