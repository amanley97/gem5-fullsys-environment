/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ArmTableWalker.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/arm/table_walker.hh"

#include "base/types.hh"
#include "sim/system.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ArmTableWalker");
    py::class_<ArmTableWalkerParams, ClockedObjectParams, std::unique_ptr<ArmTableWalkerParams, py::nodelete>>(m, "ArmTableWalkerParams")
        .def(py::init<>())
        .def("create", &ArmTableWalkerParams::create)
        .def_readwrite("is_stage2", &ArmTableWalkerParams::is_stage2)
        .def_readwrite("num_squash_per_cycle", &ArmTableWalkerParams::num_squash_per_cycle)
        .def_readwrite("sys", &ArmTableWalkerParams::sys)
        .def_readwrite("port_port_connection_count", &ArmTableWalkerParams::port_port_connection_count)
        ;

    py::class_<gem5::ArmISA::TableWalker, gem5::ClockedObject, std::unique_ptr<gem5::ArmISA::TableWalker, py::nodelete>>(m, "gem5_COLONS_ArmISA_COLONS_TableWalker")
        ;

}

static EmbeddedPyBind embed_obj("ArmTableWalker", module_init, "ClockedObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyArmTableWalkerParamsClass
{
  public:
    gem5::ArmISA::TableWalker *create() const;
};

template <class CxxClass, class Enable=void>
class DummyArmTableWalkerShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyArmTableWalkerShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ArmTableWalkerParams &>>>
{
  public:
    using Params = ArmTableWalkerParams;
    static gem5::ArmISA::TableWalker *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyArmTableWalkerShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ArmTableWalkerParams &>>>
{
  public:
    using Params = DummyArmTableWalkerParamsClass;
    static gem5::ArmISA::TableWalker *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ArmISA::TableWalker *
DummyArmTableWalkerShunt<gem5::ArmISA::TableWalker>::Params::create() const
{
    return DummyArmTableWalkerShunt<gem5::ArmISA::TableWalker>::create(*this);
}

} // namespace gem5
