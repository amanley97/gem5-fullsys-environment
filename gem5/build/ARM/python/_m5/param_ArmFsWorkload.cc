/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ArmFsWorkload.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/arm/fs_workload.hh"

#include <vector>
#include <string>
#include "base/types.hh"
#include "base/types.hh"
#include <string>
#include "base/types.hh"
#include <string>
#include "enums/ArmMachineType.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ArmFsWorkload");
    py::class_<ArmFsWorkloadParams, KernelWorkloadParams, std::unique_ptr<ArmFsWorkloadParams, py::nodelete>>(m, "ArmFsWorkloadParams")
        .def(py::init<>())
        .def("create", &ArmFsWorkloadParams::create)
        .def_readwrite("boot_loader", &ArmFsWorkloadParams::boot_loader)
        .def_readwrite("cpu_release_addr", &ArmFsWorkloadParams::cpu_release_addr)
        .def_readwrite("dtb_addr", &ArmFsWorkloadParams::dtb_addr)
        .def_readwrite("dtb_filename", &ArmFsWorkloadParams::dtb_filename)
        .def_readwrite("early_kernel_symbols", &ArmFsWorkloadParams::early_kernel_symbols)
        .def_readwrite("enable_context_switch_stats_dump", &ArmFsWorkloadParams::enable_context_switch_stats_dump)
        .def_readwrite("initrd_addr", &ArmFsWorkloadParams::initrd_addr)
        .def_readwrite("initrd_filename", &ArmFsWorkloadParams::initrd_filename)
        .def_readwrite("machine_type", &ArmFsWorkloadParams::machine_type)
        .def_readwrite("panic_on_oops", &ArmFsWorkloadParams::panic_on_oops)
        .def_readwrite("panic_on_panic", &ArmFsWorkloadParams::panic_on_panic)
        ;

    py::class_<gem5::ArmISA::FsWorkload, gem5::KernelWorkload, std::unique_ptr<gem5::ArmISA::FsWorkload, py::nodelete>>(m, "gem5_COLONS_ArmISA_COLONS_FsWorkload")
        ;

}

static EmbeddedPyBind embed_obj("ArmFsWorkload", module_init, "KernelWorkload");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyArmFsWorkloadParamsClass
{
  public:
    gem5::ArmISA::FsWorkload *create() const;
};

template <class CxxClass, class Enable=void>
class DummyArmFsWorkloadShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyArmFsWorkloadShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ArmFsWorkloadParams &>>>
{
  public:
    using Params = ArmFsWorkloadParams;
    static gem5::ArmISA::FsWorkload *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyArmFsWorkloadShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ArmFsWorkloadParams &>>>
{
  public:
    using Params = DummyArmFsWorkloadParamsClass;
    static gem5::ArmISA::FsWorkload *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ArmISA::FsWorkload *
DummyArmFsWorkloadShunt<gem5::ArmISA::FsWorkload>::Params::create() const
{
    return DummyArmFsWorkloadShunt<gem5::ArmISA::FsWorkload>::create(*this);
}

} // namespace gem5
