/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ArmPMU.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/arm/pmu.hh"

#include "base/types.hh"
#include "base/types.hh"
#include "dev/arm/base_gic.hh"
#include "dev/platform.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ArmPMU");
    py::class_<ArmPMUParams, SimObjectParams, std::unique_ptr<ArmPMUParams, py::nodelete>>(m, "ArmPMUParams")
        .def(py::init<>())
        .def("create", &ArmPMUParams::create)
        .def_readwrite("cycleEventId", &ArmPMUParams::cycleEventId)
        .def_readwrite("eventCounters", &ArmPMUParams::eventCounters)
        .def_readwrite("interrupt", &ArmPMUParams::interrupt)
        .def_readwrite("platform", &ArmPMUParams::platform)
        ;

    py::class_<gem5::ArmISA::PMU, gem5::SimObject, std::unique_ptr<gem5::ArmISA::PMU, py::nodelete>>(m, "gem5_COLONS_ArmISA_COLONS_PMU")
        .def("addEventProbe", &gem5::ArmISA::PMU::addEventProbe)
        .def("addSoftwareIncrementEvent", &gem5::ArmISA::PMU::addSoftwareIncrementEvent)
        ;

}

static EmbeddedPyBind embed_obj("ArmPMU", module_init, "SimObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyArmPMUParamsClass
{
  public:
    gem5::ArmISA::PMU *create() const;
};

template <class CxxClass, class Enable=void>
class DummyArmPMUShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyArmPMUShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ArmPMUParams &>>>
{
  public:
    using Params = ArmPMUParams;
    static gem5::ArmISA::PMU *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyArmPMUShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ArmPMUParams &>>>
{
  public:
    using Params = DummyArmPMUParamsClass;
    static gem5::ArmISA::PMU *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ArmISA::PMU *
DummyArmPMUShunt<gem5::ArmISA::PMU>::Params::create() const
{
    return DummyArmPMUShunt<gem5::ArmISA::PMU>::create(*this);
}

} // namespace gem5
