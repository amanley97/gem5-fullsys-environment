/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ArmTLB.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/arm/tlb.hh"

#include <vector>
#include "enums/ArmLookupLevel.hh"
#include "base/types.hh"
#include "sim/system.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ArmTLB");
    py::class_<ArmTLBParams, BaseTLBParams, std::unique_ptr<ArmTLBParams, py::nodelete>>(m, "ArmTLBParams")
        .def(py::init<>())
        .def("create", &ArmTLBParams::create)
        .def_readwrite("is_stage2", &ArmTLBParams::is_stage2)
        .def_readwrite("partial_levels", &ArmTLBParams::partial_levels)
        .def_readwrite("size", &ArmTLBParams::size)
        .def_readwrite("sys", &ArmTLBParams::sys)
        ;

    py::class_<gem5::ArmISA::TLB, gem5::BaseTLB, std::unique_ptr<gem5::ArmISA::TLB, py::nodelete>>(m, "gem5_COLONS_ArmISA_COLONS_TLB")
        ;

}

static EmbeddedPyBind embed_obj("ArmTLB", module_init, "BaseTLB");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyArmTLBParamsClass
{
  public:
    gem5::ArmISA::TLB *create() const;
};

template <class CxxClass, class Enable=void>
class DummyArmTLBShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyArmTLBShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ArmTLBParams &>>>
{
  public:
    using Params = ArmTLBParams;
    static gem5::ArmISA::TLB *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyArmTLBShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ArmTLBParams &>>>
{
  public:
    using Params = DummyArmTLBParamsClass;
    static gem5::ArmISA::TLB *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ArmISA::TLB *
DummyArmTLBShunt<gem5::ArmISA::TLB>::Params::create() const
{
    return DummyArmTLBShunt<gem5::ArmISA::TLB>::create(*this);
}

} // namespace gem5
