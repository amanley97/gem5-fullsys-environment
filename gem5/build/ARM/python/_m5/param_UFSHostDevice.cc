/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/UFSHostDevice.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/arm/ufs_device.hh"

#include "dev/arm/base_gic.hh"
#include <vector>
#include "dev/storage/disk_image.hh"
#include "base/types.hh"
#include "base/types.hh"
#include <vector>
#include "dev/arm/abstract_nvm.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_UFSHostDevice");
    py::class_<UFSHostDeviceParams, DmaDeviceParams, std::unique_ptr<UFSHostDeviceParams, py::nodelete>>(m, "UFSHostDeviceParams")
        .def(py::init<>())
        .def("create", &UFSHostDeviceParams::create)
        .def_readwrite("gic", &UFSHostDeviceParams::gic)
        .def_readwrite("image", &UFSHostDeviceParams::image)
        .def_readwrite("img_blk_size", &UFSHostDeviceParams::img_blk_size)
        .def_readwrite("int_num", &UFSHostDeviceParams::int_num)
        .def_readwrite("internalflash", &UFSHostDeviceParams::internalflash)
        .def_readwrite("pio_addr", &UFSHostDeviceParams::pio_addr)
        .def_readwrite("pio_latency", &UFSHostDeviceParams::pio_latency)
        .def_readwrite("ufs_slots", &UFSHostDeviceParams::ufs_slots)
        ;

    py::class_<gem5::UFSHostDevice, gem5::DmaDevice, std::unique_ptr<gem5::UFSHostDevice, py::nodelete>>(m, "gem5_COLONS_UFSHostDevice")
        ;

}

static EmbeddedPyBind embed_obj("UFSHostDevice", module_init, "DmaDevice");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyUFSHostDeviceParamsClass
{
  public:
    gem5::UFSHostDevice *create() const;
};

template <class CxxClass, class Enable=void>
class DummyUFSHostDeviceShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyUFSHostDeviceShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const UFSHostDeviceParams &>>>
{
  public:
    using Params = UFSHostDeviceParams;
    static gem5::UFSHostDevice *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyUFSHostDeviceShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const UFSHostDeviceParams &>>>
{
  public:
    using Params = DummyUFSHostDeviceParamsClass;
    static gem5::UFSHostDevice *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::UFSHostDevice *
DummyUFSHostDeviceShunt<gem5::UFSHostDevice>::Params::create() const
{
    return DummyUFSHostDeviceShunt<gem5::UFSHostDevice>::create(*this);
}

} // namespace gem5
