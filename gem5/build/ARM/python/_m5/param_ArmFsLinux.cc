/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ArmFsLinux.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/arm/linux/fs_workload.hh"

namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ArmFsLinux");
    py::class_<ArmFsLinuxParams, ArmFsWorkloadParams, std::unique_ptr<ArmFsLinuxParams, py::nodelete>>(m, "ArmFsLinuxParams")
        .def(py::init<>())
        .def("create", &ArmFsLinuxParams::create)
        ;

    py::class_<gem5::ArmISA::FsLinux, gem5::ArmISA::FsWorkload, std::unique_ptr<gem5::ArmISA::FsLinux, py::nodelete>>(m, "gem5_COLONS_ArmISA_COLONS_FsLinux")
        .def("dumpDmesg", &gem5::ArmISA::FsLinux::dumpDmesg)
        ;

}

static EmbeddedPyBind embed_obj("ArmFsLinux", module_init, "ArmFsWorkload");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyArmFsLinuxParamsClass
{
  public:
    gem5::ArmISA::FsLinux *create() const;
};

template <class CxxClass, class Enable=void>
class DummyArmFsLinuxShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyArmFsLinuxShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ArmFsLinuxParams &>>>
{
  public:
    using Params = ArmFsLinuxParams;
    static gem5::ArmISA::FsLinux *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyArmFsLinuxShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ArmFsLinuxParams &>>>
{
  public:
    using Params = DummyArmFsLinuxParamsClass;
    static gem5::ArmISA::FsLinux *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ArmISA::FsLinux *
DummyArmFsLinuxShunt<gem5::ArmISA::FsLinux>::Params::create() const
{
    return DummyArmFsLinuxShunt<gem5::ArmISA::FsLinux>::create(*this);
}

} // namespace gem5
