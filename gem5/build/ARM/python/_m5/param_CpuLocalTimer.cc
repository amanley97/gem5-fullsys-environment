/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/CpuLocalTimer.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/arm/timer_cpulocal.hh"

#include "dev/arm/base_gic.hh"
#include "dev/arm/base_gic.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_CpuLocalTimer");
    py::class_<CpuLocalTimerParams, BasicPioDeviceParams, std::unique_ptr<CpuLocalTimerParams, py::nodelete>>(m, "CpuLocalTimerParams")
        .def(py::init<>())
        .def("create", &CpuLocalTimerParams::create)
        .def_readwrite("int_timer", &CpuLocalTimerParams::int_timer)
        .def_readwrite("int_watchdog", &CpuLocalTimerParams::int_watchdog)
        ;

    py::class_<gem5::CpuLocalTimer, gem5::BasicPioDevice, std::unique_ptr<gem5::CpuLocalTimer, py::nodelete>>(m, "gem5_COLONS_CpuLocalTimer")
        ;

}

static EmbeddedPyBind embed_obj("CpuLocalTimer", module_init, "BasicPioDevice");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyCpuLocalTimerParamsClass
{
  public:
    gem5::CpuLocalTimer *create() const;
};

template <class CxxClass, class Enable=void>
class DummyCpuLocalTimerShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyCpuLocalTimerShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const CpuLocalTimerParams &>>>
{
  public:
    using Params = CpuLocalTimerParams;
    static gem5::CpuLocalTimer *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyCpuLocalTimerShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const CpuLocalTimerParams &>>>
{
  public:
    using Params = DummyCpuLocalTimerParamsClass;
    static gem5::CpuLocalTimer *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::CpuLocalTimer *
DummyCpuLocalTimerShunt<gem5::CpuLocalTimer>::Params::create() const
{
    return DummyCpuLocalTimerShunt<gem5::CpuLocalTimer>::create(*this);
}

} // namespace gem5
