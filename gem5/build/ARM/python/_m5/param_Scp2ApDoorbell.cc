/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/Scp2ApDoorbell.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/arm/css/mhu.hh"

#include "dev/arm/base_gic.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_Scp2ApDoorbell");
    py::class_<Scp2ApDoorbellParams, MhuDoorbellParams, std::unique_ptr<Scp2ApDoorbellParams, py::nodelete>>(m, "Scp2ApDoorbellParams")
        .def(py::init<>())
        .def("create", &Scp2ApDoorbellParams::create)
        .def_readwrite("interrupt", &Scp2ApDoorbellParams::interrupt)
        ;

    py::class_<gem5::Scp2ApDoorbell, gem5::MhuDoorbell, std::unique_ptr<gem5::Scp2ApDoorbell, py::nodelete>>(m, "gem5_COLONS_Scp2ApDoorbell")
        ;

}

static EmbeddedPyBind embed_obj("Scp2ApDoorbell", module_init, "MhuDoorbell");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyScp2ApDoorbellParamsClass
{
  public:
    gem5::Scp2ApDoorbell *create() const;
};

template <class CxxClass, class Enable=void>
class DummyScp2ApDoorbellShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyScp2ApDoorbellShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const Scp2ApDoorbellParams &>>>
{
  public:
    using Params = Scp2ApDoorbellParams;
    static gem5::Scp2ApDoorbell *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyScp2ApDoorbellShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const Scp2ApDoorbellParams &>>>
{
  public:
    using Params = DummyScp2ApDoorbellParamsClass;
    static gem5::Scp2ApDoorbell *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::Scp2ApDoorbell *
DummyScp2ApDoorbellShunt<gem5::Scp2ApDoorbell>::Params::create() const
{
    return DummyScp2ApDoorbellShunt<gem5::Scp2ApDoorbell>::create(*this);
}

} // namespace gem5
