/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ScmiPlatform.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/arm/css/scmi_platform.hh"

#include <vector>
#include <string>
#include "base/types.hh"
#include <string>
#include <string>
#include <vector>
#include "dev/arm/css/scmi_platform.hh"
#include "sim/system.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ScmiPlatform");
    py::class_<ScmiPlatformParams, ScpParams, std::unique_ptr<ScmiPlatformParams, py::nodelete>>(m, "ScmiPlatformParams")
        .def(py::init<>())
        .def("create", &ScmiPlatformParams::create)
        .def_readwrite("agents", &ScmiPlatformParams::agents)
        .def_readwrite("base_impl_version", &ScmiPlatformParams::base_impl_version)
        .def_readwrite("base_subvendor", &ScmiPlatformParams::base_subvendor)
        .def_readwrite("base_vendor", &ScmiPlatformParams::base_vendor)
        .def_readwrite("comms", &ScmiPlatformParams::comms)
        .def_readwrite("sys", &ScmiPlatformParams::sys)
        .def_readwrite("port_dma_connection_count", &ScmiPlatformParams::port_dma_connection_count)
        ;

    py::class_<gem5::scmi::Platform, gem5::Scp, std::unique_ptr<gem5::scmi::Platform, py::nodelete>>(m, "gem5_COLONS_scmi_COLONS_Platform")
        ;

}

static EmbeddedPyBind embed_obj("ScmiPlatform", module_init, "Scp");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyScmiPlatformParamsClass
{
  public:
    gem5::scmi::Platform *create() const;
};

template <class CxxClass, class Enable=void>
class DummyScmiPlatformShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyScmiPlatformShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ScmiPlatformParams &>>>
{
  public:
    using Params = ScmiPlatformParams;
    static gem5::scmi::Platform *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyScmiPlatformShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ScmiPlatformParams &>>>
{
  public:
    using Params = DummyScmiPlatformParamsClass;
    static gem5::scmi::Platform *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::scmi::Platform *
DummyScmiPlatformShunt<gem5::scmi::Platform>::Params::create() const
{
    return DummyScmiPlatformShunt<gem5::scmi::Platform>::create(*this);
}

} // namespace gem5
