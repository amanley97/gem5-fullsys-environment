/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ArmDecoder.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/arm/decoder.hh"

namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ArmDecoder");
    py::class_<ArmDecoderParams, InstDecoderParams, std::unique_ptr<ArmDecoderParams, py::nodelete>>(m, "ArmDecoderParams")
        .def(py::init<>())
        .def("create", &ArmDecoderParams::create)
        .def_readwrite("dvm_enabled", &ArmDecoderParams::dvm_enabled)
        ;

    py::class_<gem5::ArmISA::Decoder, gem5::InstDecoder, std::unique_ptr<gem5::ArmISA::Decoder, py::nodelete>>(m, "gem5_COLONS_ArmISA_COLONS_Decoder")
        ;

}

static EmbeddedPyBind embed_obj("ArmDecoder", module_init, "InstDecoder");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyArmDecoderParamsClass
{
  public:
    gem5::ArmISA::Decoder *create() const;
};

template <class CxxClass, class Enable=void>
class DummyArmDecoderShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyArmDecoderShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ArmDecoderParams &>>>
{
  public:
    using Params = ArmDecoderParams;
    static gem5::ArmISA::Decoder *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyArmDecoderShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ArmDecoderParams &>>>
{
  public:
    using Params = DummyArmDecoderParamsClass;
    static gem5::ArmISA::Decoder *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ArmISA::Decoder *
DummyArmDecoderShunt<gem5::ArmISA::Decoder>::Params::create() const
{
    return DummyArmDecoderShunt<gem5::ArmISA::Decoder>::create(*this);
}

} // namespace gem5
