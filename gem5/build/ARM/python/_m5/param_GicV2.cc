/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/GicV2.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/arm/gic_v2.hh"

#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_GicV2");
    py::class_<GicV2Params, BaseGicParams, std::unique_ptr<GicV2Params, py::nodelete>>(m, "GicV2Params")
        .def(py::init<>())
        .def("create", &GicV2Params::create)
        .def_readwrite("cpu_addr", &GicV2Params::cpu_addr)
        .def_readwrite("cpu_pio_delay", &GicV2Params::cpu_pio_delay)
        .def_readwrite("cpu_size", &GicV2Params::cpu_size)
        .def_readwrite("dist_addr", &GicV2Params::dist_addr)
        .def_readwrite("dist_pio_delay", &GicV2Params::dist_pio_delay)
        .def_readwrite("gem5_extensions", &GicV2Params::gem5_extensions)
        .def_readwrite("int_latency", &GicV2Params::int_latency)
        .def_readwrite("it_lines", &GicV2Params::it_lines)
        ;

    py::class_<gem5::GicV2, gem5::BaseGic, std::unique_ptr<gem5::GicV2, py::nodelete>>(m, "gem5_COLONS_GicV2")
        ;

}

static EmbeddedPyBind embed_obj("GicV2", module_init, "BaseGic");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyGicV2ParamsClass
{
  public:
    gem5::GicV2 *create() const;
};

template <class CxxClass, class Enable=void>
class DummyGicV2Shunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyGicV2Shunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const GicV2Params &>>>
{
  public:
    using Params = GicV2Params;
    static gem5::GicV2 *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyGicV2Shunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const GicV2Params &>>>
{
  public:
    using Params = DummyGicV2ParamsClass;
    static gem5::GicV2 *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::GicV2 *
DummyGicV2Shunt<gem5::GicV2>::Params::create() const
{
    return DummyGicV2Shunt<gem5::GicV2>::create(*this);
}

} // namespace gem5
