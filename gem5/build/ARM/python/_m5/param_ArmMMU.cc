/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ArmMMU.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/arm/mmu.hh"

#include "arch/arm/table_walker.hh"
#include "arch/arm/table_walker.hh"
#include "arch/arm/system.hh"
#include "arch/arm/tlb.hh"
#include "arch/arm/table_walker.hh"
#include "arch/arm/tlb.hh"
#include "arch/arm/table_walker.hh"
#include "sim/system.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ArmMMU");
    py::class_<ArmMMUParams, BaseMMUParams, std::unique_ptr<ArmMMUParams, py::nodelete>>(m, "ArmMMUParams")
        .def(py::init<>())
        .def("create", &ArmMMUParams::create)
        .def_readwrite("dtb_walker", &ArmMMUParams::dtb_walker)
        .def_readwrite("itb_walker", &ArmMMUParams::itb_walker)
        .def_readwrite("release_se", &ArmMMUParams::release_se)
        .def_readwrite("stage2_dtb", &ArmMMUParams::stage2_dtb)
        .def_readwrite("stage2_dtb_walker", &ArmMMUParams::stage2_dtb_walker)
        .def_readwrite("stage2_itb", &ArmMMUParams::stage2_itb)
        .def_readwrite("stage2_itb_walker", &ArmMMUParams::stage2_itb_walker)
        .def_readwrite("sys", &ArmMMUParams::sys)
        ;

    py::class_<gem5::ArmISA::MMU, gem5::BaseMMU, std::unique_ptr<gem5::ArmISA::MMU, py::nodelete>>(m, "gem5_COLONS_ArmISA_COLONS_MMU")
        ;

}

static EmbeddedPyBind embed_obj("ArmMMU", module_init, "BaseMMU");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyArmMMUParamsClass
{
  public:
    gem5::ArmISA::MMU *create() const;
};

template <class CxxClass, class Enable=void>
class DummyArmMMUShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyArmMMUShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ArmMMUParams &>>>
{
  public:
    using Params = ArmMMUParams;
    static gem5::ArmISA::MMU *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyArmMMUShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ArmMMUParams &>>>
{
  public:
    using Params = DummyArmMMUParamsClass;
    static gem5::ArmISA::MMU *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ArmISA::MMU *
DummyArmMMUShunt<gem5::ArmISA::MMU>::Params::create() const
{
    return DummyArmMMUShunt<gem5::ArmISA::MMU>::create(*this);
}

} // namespace gem5
